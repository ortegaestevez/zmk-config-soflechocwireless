# Copyright (c) 2020 Ryan Cross
# SPDX-License-Identifier: MIT

# Uncomment the following line to enable the Sofle OLED Display
CONFIG_ZMK_DISPLAY=y

# Battery saving
# Go to sleep after 15 minutes of inactivity
CONFIG_ZMK_IDLE_TIMEOUT=900000
# Enable the battery level peripheral to report battery status
CONFIG_ZMK_BATTERY_REPORTING=y

# Uncomment these two lines to add support for encoders
CONFIG_EC11=y
CONFIG_EC11_TRIGGER_GLOBAL_THREAD=y

CONFIG_ZMK_DISPLAY_WORK_QUEUE_DEDICATED=y

# Enables RGB functionality (Uncomment lines below to enable.)
# CONFIG_ZMK_RGB_UNDERGLOW=y
# CONFIG_WS2812_STRIP=y

# Don't turn off the key backlight when the keyboard is disconnected from USB 
# CONFIG_ZMK_BACKLIGHT_AUTO_OFF_USB=n

# Disable "Underglow toggling also controls external power" so that OLED can still run with RGB off
CONFIG_ZMK_RGB_UNDERGLOW_EXT_POWER=n
